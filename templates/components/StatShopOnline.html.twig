<div {{ attributes }}>
	<div class="container d-flex justify-content-center mt-5">
		<div class="w-50">

			<h1 class="mb-4 text-center">📊 Tableau de bord combiné</h1>

			<div class="card shadow-sm p-4 mb-4">
				<label for="months" class="form-label fw-bold mb-2">Filtrer par mois :</label>
				<select id="months" name="months" class="form-select" data-model="selectedMonths" data-controller= "symfony--ux-autocomplete--autocomplete">
					{% for m in months %}
						<option value="{{ m }}">{{ m }}</option>
					{% endfor %}
				</select>
				<small class="text-muted">Sélectionnez un ou plusieurs mois pour filtrer le graphique.</small>
			</div>

			<div class="card shadow-sm p-4">
				<h5 class="card-title mb-3">📈 Ventes & Visites</h5>
				<div style="position: relative; height:400px; width:100%;">
					{{ render_chart(showStatShopOnline) }}
				</div>
			</div>

		</div>
	</div>
</div>
